default: &default
  dispatchers:
    - polling_interval: 5
      batch_size: 100
  workers:
    - queues: "*"
      threads: 1
      processes: 1
      polling_interval: 5
      max_executions: 50

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
